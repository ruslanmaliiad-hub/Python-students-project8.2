# Автор: Маліяд Р.О.
# Завдання: створити текстовий файл і записати прізвище та питання

def main():
    try:
        with open("students_questions.txt", "w", encoding="utf-8") as file:
            file.write("Студент 1: Маліяд.Р.О.\n")
            file.write("Питання: Що таке змінна у Python і як її оголосити?\n")
        print("Файл створено і заповнено успішно.")
    except Exception as e:
        print(f"Помилка під час роботи з файлом: {e}")

if __name__ == "__main__":
    main()

# Автор: Головчук Н.В 
# Завдання: Дописати у файл відповідь на питання Студента 1 та поставити своє питання

def main():
    try:
        with open("students_questions.txt", "a", encoding="utf-8") as file:
            file.write("\nСтудент 2: Головчук Н.В\n")

            file.write("Відповідь: Змінна у Python — це іменована область пам'яті, "
                       "у якій зберігається значення, яке може змінюватися. "
                       "Щоб оголосити змінну, достатньо присвоїти їй значення через оператор '='. "
                       "Наприклад: x = 10\n")
            file.write("Питання: Що таке список (list) у Python і як додати до нього елемент?\n")

        print("Дані успішно дописані у файл.")
    except FileNotFoundError:
        print("Помилка: файл students_questions.txt не знайдено.")
    except Exception as e:
        print(f"Сталася помилка під час роботи з файлом: {e}")

if __name__ == "__main__":
    main()

# Автор: Швачко Є.В
# Завдання: Дописати у файл відповідь на питання Студента 2 та поставити своє питання

def main():
    try:
        with open("students_questions.txt", "a", encoding="utf-8") as file:
            file.write("\n\nСтудент 3: Швачко Є.В.\n")
            
            file.write("Відповідь: Список (list) — це впорядкована змінна структура даних, яка може містити елементи різних типів. "
                       "Для додавання елемента в кінець списку використовується метод .append().\n")
            file.write("Питання: Що таке словник (dict) у Python і яка його головна особливість зберігання даних?\n")
            
            print("Дані успішно дописані у файл.")
    except Exception as e:
        print(f"Сталася помилка під час роботи з файлом: {e}")

if __name__ == "__main__":
    main()

# Автор: Строгий Д.Р
# Завдання: Дописати у файл відповідь на питання Студента 3 та поставити своє питання

def main():
    try:
        with open("students_questions.txt", "a", encoding="utf-8") as file:
            file.write("\n\nСтудент 4: Строгий Д.Р.\n")
            
            file.write("Відповідь: Словник (dict) — це структура даних для зберігання пар «ключ–значення». "
                       "Головна особливість — доступ до даних здійснюється за унікальним ключем.\n")
            file.write("Питання: У чому різниця між використанням with open(...) та звичайним відкриттям файлу через open() у Python?\n")            
            print("Дані успішно дописані у файл.")
    except Exception as e:
        print(f"Сталася помилка під час роботи з файлом: {e}")

if __name__ == "__main__":
    main()

